package com.workmanagement.app;

import com.getcapacitor.BridgeActivity;

public class MainActivity extends BridgeActivity {
    // TextRecognitionPlugin should be auto-discovered via @CapacitorPlugin annotation
    // Make sure to:
    // 1. Run "npx cap sync android" 
    // 2. Clean and rebuild in Android Studio (Build > Clean Project, then Rebuild)
    // 3. The @CapacitorPlugin annotation on TextRecognitionPlugin will handle registration
}
