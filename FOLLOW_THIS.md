# 👉 FOLLOW THIS - Simple Step-by-Step

## 🎯 STEP 1: Open Terminal/Command Prompt

You need to open a terminal window to run commands.

### How to Open Terminal:

**Option A: In Cursor (Recommended)**
1. Look at the bottom of Cursor
2. Click on **"Terminal"** tab
3. OR press `Ctrl + ~` (Control + Tilde key)

**Option B: Windows Command Prompt**
1. Press `Windows Key + R`
2. Type `cmd` and press Enter
3. Navigate to your project folder:
   ```bash
   cd "C:\Users\King\Desktop\TCB Metal Works\BOIE COPY\nextjs-auth-roles - Copy"
   ```

**Option C: PowerShell**
1. Press `Windows Key + X`
2. Click "Windows PowerShell"
3. Navigate to your project folder:
   ```powershell
   cd "C:\Users\King\Desktop\TCB Metal Works\BOIE COPY\nextjs-auth-roles - Copy"
   ```

---

## 🎯 STEP 2: Run This Command

In the terminal, type this command and press Enter:

```bash
npm run dev
```

**What you'll see:**
- It will start the development server
- You'll see something like: "Ready in 2.3s"
- It will show: "Local: http://localhost:3000"

**Keep this terminal window open!** The server needs to keep running.

---

## 🎯 STEP 3: Open Your Browser

1. Open any web browser (Chrome, Edge, Firefox, etc.)
2. Go to this address: **http://localhost:3000**
3. You should see the app!

---

## ✅ That's It!

You're now running the app locally. You can:
- See the website
- Login with test accounts
- Make changes to the code
- See changes instantly

---

## 🔑 To Login

Use these test accounts:
- **Admin:** `admin@example.com` / `Passw0rd!`
- **Manager:** `manager@example.com` / `Passw0rd!`
- **Employee:** `employee@example.com` / `Passw0rd!`

---

## 📍 Where Am I?

**Current Location:**
- Project folder: `C:\Users\King\Desktop\TCB Metal Works\BOIE COPY\nextjs-auth-roles - Copy`
- This is where you need to run the commands

**To verify you're in the right place:**
- In terminal, type: `pwd` (PowerShell) or `cd` (Command Prompt)
- Should show your project folder path

---

## 🆘 If You Get Stuck

**"npm is not recognized"**
- Make sure Node.js is installed
- Restart your terminal after installing Node.js

**"Port 3000 already in use"**
- Run: `npm run dev -- -p 3001`
- Then go to: `http://localhost:3001`

**"Cannot find module"**
- Run: `npm install`
- Then try `npm run dev` again

---

## 🎯 Quick Summary

1. ✅ Open Terminal (in Cursor: `Ctrl + ~`)
2. ✅ Type: `npm run dev`
3. ✅ Open browser: `http://localhost:3000`
4. ✅ Start coding!

**That's all you need!** 🚀

